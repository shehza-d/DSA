//Tezeract JS practice
Number("0.454"); //0.454

Math.abs(-93); // 93


"string".includes(st)
"string".contains(st)

//for sorting array in numbers
const arr = [34, 32, 1, 33, 102, 56, 78, 6, 4];
arr.sort((x, y) => x - y); //ascending
arr.sort((x, y) => y - x); //descending

//instead of if(myVar)   because this can return error
if (typeof (myVar !== "undefined")) {
}

// map jitni bar chale ga apne har return ko array ma save karky return kare ga
// forEach return nhi karta params(item,index,full-array)
//for in loop ma object literal ki keys milti hyn
//for off loop


if(window.location.protocol)//for front-end condition

document.links
document.images
document.scripts 
document.scripts[0].getAttribute('src') 

//convert HTML collection in array before manipulation  [Array.from(**)]
// d/b node list and HTML collection 

.indexOf()//rescersh more

// make guessGame in CLI 
// with 3 tries and msg of closes guess and after losing tell wining number
// `8 is not correct 2 guess left`
let max=16,min=4;
Math.floor(Math.random()*(max-min+1)+min);//to get a random number between max and min like 
